# M0.2 GOAL contract

Base: 7a76ebcb4f1aa7d3027f84a9c1b8f48aaa0292b4.
OWNER accepted; no provider/startup redesign.

| Contract | Allowed | Denied / race evidence |
| --- | --- | --- |
| Owner preparation | Explicit requested goal only; existing templates/envelope | Missing/not-ready/wrong-root/stale generation; no fallback |
| Identity | Bounded request key and payload digest; exact mapping | Changed payload; cross-kind key; second-key duplicate goal |
| Authority | Preserve stored templates, budgets, job-pack/priming references | Unexpected RPC/CLI parameters; invalid envelope/binding; unknown/blocked goal |
| Atomic visibility | Missions remain preparing until terminal commit | Collisions preflighted; partial/uncertain missions cannot execute |
| Interruption | Durable quarantine with exact request mapping | No replay/overwrite after multi-mission interruption |
| Production consumers | CLI enqueue -> prepare -> run one synthetic step -> reconnect/result | No controller.step or hand-created missions for happy path; no unrelated draining |
| Cleanup | Finite Linux fixtures and independent watchdog | Any intervention or active remainder fails |

Preparation is deterministic metadata-only work, never objective completion.
Focused parser and actual Linux CLI/daemon proofs precede final full-suite CI.
