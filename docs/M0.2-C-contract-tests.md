# M0.2-C contract-to-test plan

Base: `37c27bc787e074fd11a0fcaed7d069e507830044`. No host sandbox execution.

| Contract | Allowed | Denied / race / failure | Proof |
| --- | --- | --- | --- |
| Trusted configuration | Explicit worker/reviewer adapter profiles via direct API or operator JSON path | Missing role, mission grant injection, unsupported real profile; no generating spawn | Controller admission spies; CLI/daemon consumers |
| Separate roles | Worker roots intersect trusted grants and exact mission worktree; read-only reviewer, role-private state | Reviewer writes, outside reads, shared role session, job widening tools/roots/budgets | Actual controller -> shared adapter -> Linux fixture filesystem/argv |
| Durable session | Atomic mission/role/session/digest record; settled same-profile resume with fresh adapter/controller | Changed or legacy settled session resets affected role only; running/uncertain session blocks | Journal reconstruction and actual resumed invocation |
| Terminal truth | Effective digest and terminal cause in attempt receipt | Typed policy/unsupported refusal never review success or repair churn; cleanup failure durably blocks | Pre-spawn refusals, owned process receipts, cleanup-failure injection across restart |
| Regression | Accepted R1/R2 and M0.1/M1 tests retained | No live provider, host containment, candidate/release or hardware acceptance claim | Focused checks; final npm test + disposable Linux CI |

Legacy fake-controller tests receive explicit test-only trusted profiles; they
remain regression tests, not evidence closing process-level integration.
