# M0.2 R1/R2 contract-to-test plan

Before: `5b05be8cab178a6a551831d6797195240fe0ad34`.
No macOS sandbox execution; compilation-only checks on the host. Actual process
and filesystem proofs run on the existing Ubuntu/bubblewrap CI executor.

| Contract | Allowed | Denied/failure | Consumer proof |
| --- | --- | --- | --- |
| Explicit denial | Read with unrelated denial; identical-policy resume | Allow Read + deny Read removes availability/preapproval; changed denial invalidates old session | Shared adapter start/resume, actual Claude-protocol fixture argv in Linux |
| Denial input | Union of policy/config/job exact-name denials | Malformed lists, patterns, unsupported adapter enforcement rejected before spawn | Compiler plus adapter and channel envelope propagation |
| Linux filesystem | Read/write root contained in granted read root; read-only and zero-data profiles | Empty reads + write root; disjoint roots; parent write over narrowed child read | Actual baseline leak; repaired refusal; allowed/denied file syscalls in supported Linux profiles |
| Regression | Existing output/lifecycle and M0.1/M1 guards | Unsupported real profiles stay refused | Focused checks then final npm test/CI; explicit NOT_TESTED for host sandbox tests |

Provider table is a decision artifact. CLI help is not live acceptance. Preserve
the six-process incident; read-only status must include start time and identity.
